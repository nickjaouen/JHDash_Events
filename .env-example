OPENAI_API_KEY=
TAVILY_API_KEY=
#Currently unused, previously used for checkpointing
POSTGRES_URL="postgres://postgres:postgres@localhost:5432/jhdash_agents"
# Target for events DB
POSTGRES_URL_2=

# Comma-separated URLs (or one URL per line in the quoted value). Not Python list syntax.
ACTIVE_STARTING_PAGE_URLS=

